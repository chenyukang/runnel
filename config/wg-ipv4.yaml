# Sample WG IPv4 config.
# Prefer generating a fresh config with `runnel wg-config`.
# Replace every REPLACE_WITH_* value before running.

log: info
log_file: ./logs/runnel-wg-ipv4.log
telemetry_sock: ./run/runnel-wg-ipv4.sock
tui: true

client:
  mode: wg
  adblock:
    enabled: true
    lists:
      - https://easylist.to/easylist/easylist.txt
      - https://easylist.to/easylist/easyprivacy.txt
      - https://raw.githubusercontent.com/uBlockOrigin/uAssets/master/filters/filters.txt
    cache_dir: ~/.cache/runnel/adblock
    update_interval_hours: 24
    decision_cache_ttl_secs: 300
    fail_open: true
  ip_rules:
    direct:
      - "10.*"
      - "172.16.*"
      - "172.17.*"
      - "172.18.*"
      - "172.19.*"
      - "172.20.*"
      - "172.21.*"
      - "172.22.*"
      - "172.23.*"
      - "172.24.*"
      - "172.25.*"
      - "172.26.*"
      - "172.27.*"
      - "172.28.*"
      - "172.29.*"
      - "172.30.*"
      - "172.31.*"
      - "192.168.*"
      - "169.254.*"
  wg:
    # engine: noise
    endpoint: SERVER-IP:1443
    private_key: REPLACE_WITH_CLIENT_PRIVATE_KEY
    peer_public_key: REPLACE_WITH_SERVER_PUBLIC_KEY
    tunnel_ip: 10.8.0.2
    peer_tunnel_ip: 10.8.0.1
    mtu: 1420
    persistent_keepalive_secs: 25
    dns: 1.1.1.1
    dns_capture: true
    # tcpdump: true
    # tcpdump_interface: any

server:
  mode: wg
  wg:
    # engine: noise
    listen: 0.0.0.0:1443
    private_key: REPLACE_WITH_SERVER_PRIVATE_KEY
    peer_public_key: REPLACE_WITH_CLIENT_PUBLIC_KEY
    tunnel_ip: 10.8.0.1
    peer_tunnel_ip: 10.8.0.2
    mtu: 1420
    peer_allowed_ips:
      - 10.8.0.2/32
    nat_out_interface: eth0
    # tcpdump: true
    # tcpdump_interface: eth0
